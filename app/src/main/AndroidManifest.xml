<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dicoding.android.fundamental.githubuserapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <permission
        android:name="com.dicoding.android.fundamental.githubuserapp.READ_DATABASE"
        android:protectionLevel="normal" />
    <permission
        android:name="com.dicoding.android.fundamental.githubuserapp.WRITE_DATABASE"
        android:protectionLevel="normal" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.RemainderActivity"></activity>

        <receiver
            android:name=".Receiver.MyReceiver"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".activity.FavouriteActivity" />
        <activity android:name=".activity.DetailUser" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name=".Provider"
            android:authorities="com.dicoding.android.fundamental.githubuserapp"
            android:enabled="true"
            android:exported="true"
            android:readPermission="com.dicoding.android.fundamental.githubuserapp.READ_DATABASE"
            android:writePermission="com.dicoding.android.fundamental.githubuserapp.WRITE_DATABASE" />
    </application>

</manifest>